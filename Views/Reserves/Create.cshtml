@model DbLabProject.Models.ReserveViewModel

@{
    ViewData["Title"] = "Reserve";
}

<h1>Reserve Food</h1>

<h4>Reserve</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RestaurantName" class="control-label"></label>
                <select asp-for="RestaurantName" class ="form-control" asp-items="ViewBag.RestaurantName"></select>
            </div>
            <div class="form-group">
                <label asp-for="FoodName" class="control-label"></label>
                <select asp-for="FoodName" class ="form-control" asp-items="ViewBag.FoodName"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
